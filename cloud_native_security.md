# 云原生安全最佳实践

## 容器安全

### 镜像安全
- 使用最小化基础镜像（Alpine、Distroless）
- 定期扫描镜像漏洞，使用 Trivy、Clair 等工具
- 对镜像进行数字签名，确保来源可信
- 避免在镜像中硬编码密钥和敏感信息

### 运行时安全
- 以非 root 用户运行容器
- 启用只读根文件系统
- 限制容器资源（CPU、内存）
- 使用 seccomp 和 AppArmor/SELinux profile
- 监控容器异常行为，如进程注入、权限提升

## Kubernetes 安全

### 网络策略
- 默认拒绝所有流量，按需开放
- 使用 NetworkPolicy 实现微隔离
- 限制 Pod 间通信范围

### RBAC 权限控制
- 遵循最小权限原则
- 定期审计权限分配
- 避免使用 cluster-admin
- 为每个服务账户分配独立权限

### 密钥管理
- 使用 Secrets 或外部密钥管理（Vault、AWSCSM）
- 避免在环境变量中明文存储密钥
- 启用密钥加密存储

## 供应链安全

### CI/CD 安全
- 实施软件物料清单（SBOM）
- 对依赖包进行漏洞扫描
- 启用镜像签名验证
- 防止构建注入攻击

### 依赖管理
- 使用私有镜像仓库
- 锁定依赖版本，定期更新
- 监控已知漏洞数据库（CVE）

## 零信任架构

- 始终验证，从不信任
- 基于身份的访问控制
- 持续监控和评估信任等级
- 微隔离限制横向移动

## 日志与监控

- 集中收集容器和集群日志
- 实时监控异常行为
- 建立告警规则和响应流程
- 保留日志用于事后审计
